<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/home_title"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/btnNormalPractice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_normal_practice"
            android:textSize="15sp"
            android:textColor="@color/destructive"
            android:background="@drawable/bg_btn_red_outline"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btnTimedPractice"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_timed_practice"
            android:textSize="15sp"
            android:textColor="@color/destructive"
            android:background="@drawable/bg_btn_red_outline"
            android:padding="14dp"
            android:layout_marginBottom="12dp" />

        <Button
            android:id="@+id/btnAddContent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/btn_add_content"
            android:textSize="15sp"
            android:textColor="@color/destructive"
            android:background="@drawable/bg_btn_red_outline"
            android:padding="14dp" />

    </LinearLayout>

</ScrollView>
